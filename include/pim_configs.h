#ifndef PIM_CONFIG_H
#define PIM_CONFIG_H

// Size of a vector that a PIM unit can process
#define ELEMENTS_PER_BANK 16

#define NUM_BANKS 16

// The size a single PIM-UNIT can process in bits
#define CHUNK_SIZE_BITS 256

#endif